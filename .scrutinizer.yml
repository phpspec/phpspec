imports:
  - javascript
  - php

tools:
  php_code_sniffer:
    filter:
      excluded-paths: [ spec/*, integration/* ]

  php_analyzer:
    filter:
      excluded-paths: [ spec/*, integration/* ]

  php_sim:
    filter:
      excluded-paths: [ spec/*, integration/* ]

filter:
  excluded_paths:
    - docs/*
    - vendor/*
